export type LaporanColumn = {
  key: string;
  title: string;
};

// Placeholder columns — adjust as needed
export const laporanColumns: LaporanColumn[] = [
  { key: "id", title: "ID" },
  { key: "judul", title: "Judul" },
  { key: "tanggal", title: "Tanggal" },
];

